# AICALCULATOR

โปรเจกต์เว็บสเตติก (HTML/CSS/JS) สำหรับวิเคราะห์และคำนวณตัวเลข (AI Calculator — Collapsible Pair Follow)

คำอธิบาย
- เว็บนี้เป็นเว็บลูกเล่นฝั่ง client ที่ใช้ HTML/CSS/JavaScript ในการประมวลผลสถิติและวิเคราะห์เลขย้อนหลัง (ไม่มี server-side)

วิธีใช้งาน (รวบรัด)
1. เปิดหน้าเว็บ: GitHub Pages จะให้ URL ในรูปแบบ https://somrid.github.io/AICALCULATOR
2. วางหรือแก้ไขข้อมูลสถิติในช่อง "ฐานข้อมูล" (input box) ตามรูปแบบตัวเลข เช่น 12345678 (หรือ 5+2 รูปแบบที่ซึ่งฟังก์ชัน parse จะอ่านได้)
3. กด "เพิ่ม + คำนวณ" เพื่อเติมงวดใหม่ หรือกด "🚀 คำนวณใหม่" เพื่อรันการวิเคราะห์
4. ผลลัพธ์จะถูกแสดงเป็นตารางและแดชบอร์ดภายในหน้าเดียว

การ deploy บน GitHub Pages
- โค้ดนี้ถูกตั้งให้เสิร์ฟจาก branch `main` ที่ root ของ repository (Project site)
- หาก repo ชื่อ `AICALCULATOR` อยู่ที่บัญชี `somrid` URL จะเป็น:
  https://somrid.github.io/AICALCULATOR
- หากต้องการใช้โดเมนของตัวเอง ให้เพิ่มไฟล์ `CNAME` หรือทำการตั้งค่าใน Settings → Pages

ข้อควรระวัง
- ไฟล์นี้เก็บข้อมูลบน localStorage ของเบราว์เซอร์ หากต้องการสำรองข้อมูล ให้คัดลอกจากช่องฐานข้อมูลและเก็บเป็นไฟล์สำรอง
- ห้ามเก็บข้อมูลลับ (API keys, passwords) ใน repository

ไฟล์ใน repo
- index.html — หน้าเว็บทั้งหมด (client-side)
- README.md — คำอธิบายและวิธีใช้งาน

ผู้สร้าง: somrid